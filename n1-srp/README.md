# 🟢 Nível 1: SRP

O `validadorUsuario` mistura validações de e-mail, senha e idade. Sua missão é separar essas responsabilidades.

### Tarefas:
- [ ] Criar `EmailValidator` (com testes).
- [ ] Criar `PasswordValidator` (mínimo 8 caracteres, com testes).
- [ ] Refatorar `validadorUsuario` para coordenar os novos validadores.

### Como rodar:
```bash
npm install && npm test
```
